-Create a nodeJS webserver that converts Markdown to HTML and serve it in a webpage

Tools to use: 
    -showdownjs
    -github-markdown-css
    -express

After building the HTML file, build it and serve it with nginx
